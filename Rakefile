
namespace :db do
  task :load_config do
    # TODO add task to load DB
  end
end

namespace :devops do
  desc "Starts the thin web server through rackup."
  task :start_app do
    `rackup -p 2000`
  end
end